{
  "specVersion": 1,
  "name": "minimal",
  "version": "1.0.0",
  "displayName": "Minimal",
  "description": "Clean, quiet, typography-first theme with plenty of whitespace.",
  "author": "GitPress",
  "license": "MIT",
  "homepage": "https://github.com/tap6/gitpress",
  "engine": "astro",
  "preview": "preview.svg",
  "tags": ["minimal", "sans-serif", "clean"],
  "configSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "accentColor": {
        "type": "string",
        "format": "color",
        "default": "#2563eb",
        "description": "Accent color for links and highlights."
      },
      "showExcerpts": {
        "type": "boolean",
        "default": false,
        "description": "Show post descriptions in the post list."
      },
      "showLogo": {
        "type": "boolean",
        "default": true,
        "description": "Show the site logo in the header when one is uploaded."
      },
      "showAvatar": {
        "type": "boolean",
        "default": false,
        "description": "Show the site avatar in the header when one is uploaded."
      },
      "showTitle": {
        "type": "boolean",
        "default": true,
        "description": "Show the site name next to the logo."
      },
      "showTagline": {
        "type": "boolean",
        "default": false,
        "description": "Show the site description under the title."
      },
      "showSearch": {
        "type": "boolean",
        "default": true,
        "description": "Show a Search link in the header."
      }
    },
    "additionalProperties": true
  }
}
